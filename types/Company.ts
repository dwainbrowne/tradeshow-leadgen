// types/Company.ts

// TypeScript type for the company data based on the provided JSON structure
export type Company = {
  id: number;
  companyName: string;
  category: string;
  website: string;
  contact: string;
  location: string;
  notes: string;
  priority: number;
  assigned: string;
};
